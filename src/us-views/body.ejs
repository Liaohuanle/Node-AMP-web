
  <body>
    <%- include('../components/nav/nav' , { langTransfor, navList }) %>
    <section class='main'>
      <div class='main-container'>
        <%- include('../components/banner/banner', {
            bannerVideoPoster,
            bannerVideoUrl,
            bannerBgUrl,
            spanText: JSON.parse(setIntervalTimeNode).spanText,
            bannerImgPC,
            bannerImgMobile,
            langTransfor
          }) %>
        <%- include('./component/oneMIllionResume/oneMillionresume') %>
        <%- include('./component/about/about', {
            currentStep: JSON.parse(aboutPanelTimeNode).currentStep,
            spanText: JSON.parse(aboutPanelTimeNode).spanText,
            aboutTagList,
            setIntervalTimeNode: aboutPanelTimeNode,
            titleList,
            rule,
            aboutSubList,
            prizeArticle
          }) %>
        <%- include('../components/winners/winners', { winnerList, topFiveWinners }) %>
        <%- include('../components/video/video', { videoUrl }) %>
        <%- include('../components/termofuser/termofuse') %>
      </div>
    </section>
  </body>

  