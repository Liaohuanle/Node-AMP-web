<!doctype html>

<html>
  <%- include('../components/header', {
   cssUrl 
  }) %>
  <%- include('./body', {
    bannerBgUrl,
    setIntervalTimeNode,
    aboutPanelTimeNode,
    aboutTagList,
    bannerImgPC,
    bannerImgMobile,
    langTransfor,
    titleList,
    navList,
    topFiveWinners,
    rule,
    prizeArticle,
    aboutSubList,
    winnerList,
  }) %>
  <%- include('../components/run',  {
      setIntervalTimeNode,
      aboutPanelTimeNode,
      YTvideoinfo,
      ajaxUrl,
      ruleAjaxUrl
    })
  %>
</html> 