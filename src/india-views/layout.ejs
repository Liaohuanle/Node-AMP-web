<!doctype html>

<html>
  <%- include('../components/header', {
   cssUrl
  }) %>
  <%- include('./body', {
    noBannerVideo,
    setIntervalTimeNode,
    aboutPanelTimeNode,
    aboutTagList,
    bannerImgPC,
    bannerImgMobile,
    langTransfor,
    langText,
    titleList,
    navList,
    topFiveWinners,
    rule,
    prizeArticle,
    aboutSubList,
    winnerList,
  }) %>
  <%- include('../components/run',  {
      setIntervalTimeNode,
      aboutPanelTimeNode,
      YTvideoinfo,
      ajaxUrl,
      ruleAjaxUrl
    })
  %>
</html> 