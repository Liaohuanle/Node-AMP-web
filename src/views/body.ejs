
  <body>
    <%- include('./component/nav/nav' , { langTransfor, langText }) %>
    <section class='main'>
      <div class='main-container'>
        <%- include('./component/banner/banner', {
            spanText: JSON.parse(setIntervalTimeNode).spanText,
            bannerImgPC,
            bannerImgMobile,
            langTransfor
          }) %>
        <%- include('./component/about/about', {
            currentStep: JSON.parse(setIntervalTimeNode).currentStep,
            spanText: JSON.parse(setIntervalTimeNode).spanText,
            aboutTagList,
            setIntervalTimeNode,
            titleList
          }) %>
      </div>
    </section>
  </body>