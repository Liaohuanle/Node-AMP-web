
  <body>
    <%- include('./component/nav/nav' , { langTransfor, langText, navList }) %>
    <section class='main'>
      <div class='main-container'>
        <%- include('./component/banner/banner', {
            spanText: JSON.parse(setIntervalTimeNode).spanText,
            bannerImgPC,
            bannerImgMobile,
            langTransfor
          }) %>
        <%- include('./component/winners/winners', { videoUrl }) %>
        <%- include('./component/about/about', {
            currentStep: JSON.parse(aboutPanelTimeNode).currentStep,
            spanText: JSON.parse(aboutPanelTimeNode).spanText,
            aboutTagList,
            setIntervalTimeNode: aboutPanelTimeNode,
            titleList,
            rule,
            aboutSubList,
            prizeArticle
          }) %>
        <%- include('./component/video/video', { videoUrl }) %>
        <%- include('./component/youcanmeet/youcanmeet', { meetUserList, titleList, aboutSubList }) %>
        <%- include('./component/termofuser/termofuse') %>
      </div>
    </section>
  </body>

  